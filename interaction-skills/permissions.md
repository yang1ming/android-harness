# Permission Dialogs

Use `ui_tree()` or `visible_texts()` to find Android permission buttons such as
"Allow", "While using the app", or localized equivalents. Prefer tapping the
button bounds from the UI tree when available; otherwise use screenshot-guided
coordinates.
